import XCTest
@testable import TKFormTextField

final class TKFormTextFieldTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TKFormTextField().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
